import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
    fonts: {
        heading: "Rampart One, sans-serif",
        body: "Rampart One, sans-serif",
    },
})

export default theme